# Project name drives container/volume/image prefixes. Set explicitly so they
# read `reverie-*` regardless of the folder the repo is checked out into.
name: reverie

services:
  backend-spring:
    build: ./backend-spring
    ports:
      - "8080:8080"
    environment:
      # Postgres is Neon. The JDBC form of Neon's DIRECT URL -- the host
      # WITHOUT `-pooler` -- with `channel_binding` stripped, that being a
      # libpq parameter the JDBC driver does not understand and rejects.
      #
      # Direct because row-level security is armed with a session-level setting
      # on each connection (TenantAwareDataSource), and a transaction-mode
      # pooler gives the next transaction a different server connection: the
      # tenant is lost and the API answers with an empty account rather than an
      # error. DeploymentCheck refuses to start on a `-pooler` URL.
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL:?set it in .env}
      # Runtime: unprivileged, so row-level security binds.
      SPRING_DATASOURCE_USERNAME: ${APP_DB_USER:-reverie_app}
      SPRING_DATASOURCE_PASSWORD: ${APP_DB_PASSWORD:-reverie_app}
      # System pool: BYPASSRLS, used only for worker callbacks, the outbox
      # relay, worker callbacks, share links and provisioning. Routed to by
      # connection, so no statement can grant itself this privilege.
      REVERIE_DATASOURCE_SYSTEM_USERNAME: ${SYS_DB_USER:-reverie_sys}
      REVERIE_DATASOURCE_SYSTEM_PASSWORD: ${SYS_DB_PASSWORD:-reverie_sys}
      # Migrations: schema owner, and the DIRECT endpoint rather than the
      # pooled one. Flyway holds an advisory lock across several transactions
      # and a transaction-mode pooler does not keep it, so migrating through
      # the pooler can deadlock or half-apply.
      FLYWAY_URL: ${FLYWAY_URL:?set it in .env}
      FLYWAY_USER: ${FLYWAY_USER:-reverie}
      FLYWAY_PASSWORD: ${FLYWAY_PASSWORD:-reverie}
      # Defaults to the compose broker. Set KAFKA_BOOTSTRAP_SERVERS plus the
      # SASL vars in .env to point local dev at a managed broker instead —
      # worth doing once before a deploy, because credentials that are wrong
      # here fail the same way they would on Render, where the logs are worse.
      # One .env name feeds both services; only the container env differs.
      SPRING_KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS:?set it in .env}
      KAFKA_SECURITY_PROTOCOL: ${KAFKA_SECURITY_PROTOCOL:-PLAINTEXT}
      KAFKA_SASL_MECHANISM: ${KAFKA_SASL_MECHANISM:-PLAIN}
      KAFKA_SASL_JAAS_CONFIG: ${KAFKA_SASL_JAAS_CONFIG:-}
      # Defaults to clerk because the alternative trusts a header -- see
      # AuthModeDefaultTest. Note what that costs since V69: clerk mode refuses
      # to START without CLERK_SECRET_KEY and FREE_TIER_IDENTITY_HMAC_SECRET,
      # because the lifetime free allowance is enforced against a verified Clerk
      # identity and cannot be without them (ClerkIdentityCheck). Set both below,
      # or run REVERIE_AUTH_MODE=dev, which is the zero-configuration path and
      # needs neither.
      REVERIE_AUTH_MODE: ${REVERIE_AUTH_MODE:-clerk}
      # The development value now lives HERE and only here. application.yml has
      # no default for it any more: an unset REVERIE_INTERNAL_TOKEN used to
      # resolve to this same published string, which made a deployment that
      # never configured it indistinguishable from one that did. Blank means
      # InternalTokenFilter refuses every /internal/** callback.
      #
      # Which is why compose still passes it explicitly -- the local stack is
      # meant to work out of the box, and this token is public anyway.
      REVERIE_INTERNAL_TOKEN: ${REVERIE_INTERNAL_TOKEN:-dev-internal-token}
      # Clerk's Backend API credential. The backend uses it for one call:
      # resolving the verified primary email of a token subject, so the lifetime
      # free allowance has a durable identity without needing a JWT template
      # (V69, ClerkDirectory).
      #
      # REQUIRED IN CLERK MODE, which is this file's default: the app refuses to
      # start without it rather than serving 500s once somebody signs in. Unset
      # is fine in dev mode, which has no Clerk at all and derives a stable
      # identity from the dev subject instead.
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY:-}
      # The free-tier identity key (V69). No default, here or anywhere: a
      # generated one would be new on every restart, which is the reset bug with
      # extra steps -- every returning identity looks new and gets another 100
      # minutes, silently.
      #
      # REQUIRED IN CLERK MODE, which is this file's default; the app refuses to
      # start without it (ClerkIdentityCheck), and a production deployment is
      # refused twice over (DeploymentCheck also rejects the published
      # development value). In dev mode it is not needed at all:
      # `FreeTierIdentityHasher` uses a fixed published key there, which is safe
      # for the same reason dev mode is -- dev mode already trusts an X-Dev-User
      # header -- and is *fixed* rather than random, so restarting the local
      # stack does not hand the local account another 100 minutes.
      FREE_TIER_IDENTITY_HMAC_SECRET: ${FREE_TIER_IDENTITY_HMAC_SECRET:-}
      # The nightly retention pass (V35). Exposed for the same reason and one
      # more: it is the only scheduled job that deletes data, so an environment
      # restored from a backup needs to be able to switch it off before it wakes
      # up and re-runs last month's deletions against data somebody is trying to
      # recover.
      REVERIE_RETENTION_CRON: "${REVERIE_RETENTION_CRON:-0 0 3 * * *}"
      REVERIE_RETENTION_ENABLED: "${REVERIE_RETENTION_ENABLED:-true}"
      CLERK_ISSUER: ${CLERK_ISSUER:-}
      CLERK_JWKS_URL: ${CLERK_JWKS_URL:-}
      AI_SERVICE_URL: http://ai-service:8000
      # Object storage is Cloudflare R2. No local fallback: a default onto a
      # host that no longer exists fails as a connection timeout rather than
      # as the missing setting it actually is.
      S3_ENDPOINT: ${S3_ENDPOINT:?set it in .env}
      # The same host, unlike under MinIO -- R2's endpoint is reachable from
      # the internet, so a browser can follow a presigned URL straight to it.
      S3_PUBLIC_ENDPOINT: ${S3_PUBLIC_ENDPOINT:?set it in .env}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY:?set it in .env}
      S3_SECRET_KEY: ${S3_SECRET_KEY:?set it in .env}
      S3_BUCKET: ${S3_BUCKET:-reverie}
      # R2 signs with this and wants `auto`. MinIO ignored it, which is why it
      # was never in this file before.
      S3_REGION: ${S3_REGION:-auto}
      # The only allowed CORS origin and the only allowed STOMP origin. Was
      # hardcoded, so a stack served from anywhere but localhost:3000 could not
      # be fixed without editing this file.
      APP_FRONTEND_URL: ${APP_FRONTEND_URL:-http://localhost:3000}
      # Where this API is reachable from OUTSIDE the docker network. Only the
      # calendar feed uses it, and that is fetched by Google's and Apple's
      # servers -- so localhost is right here and wrong anywhere real.
      APP_PUBLIC_URL: ${APP_PUBLIC_URL:-http://localhost:8080}

  ai-service:
    build: ./ai-service
    ports:
      - "8000:8000"
    environment:
      KAFKA_BOOTSTRAP_SERVERS: ${KAFKA_BOOTSTRAP_SERVERS:?set it in .env}
      KAFKA_SECURITY_PROTOCOL: ${KAFKA_SECURITY_PROTOCOL:-PLAINTEXT}
      KAFKA_SASL_MECHANISM: ${KAFKA_SASL_MECHANISM:-PLAIN}
      KAFKA_SASL_USERNAME: ${KAFKA_SASL_USERNAME:-}
      KAFKA_SASL_PASSWORD: ${KAFKA_SASL_PASSWORD:-}
      SPRING_CALLBACK_URL: http://backend-spring:8080
      REVERIE_INTERNAL_TOKEN: ${REVERIE_INTERNAL_TOKEN:-dev-internal-token}
      AI_PROVIDER: ${AI_PROVIDER:-mock}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_TRANSCRIBE_MODEL: ${OPENAI_TRANSCRIBE_MODEL:-whisper-1}
      OPENAI_CHAT_MODEL: ${OPENAI_CHAT_MODEL:-gpt-5.6-terra}
      # Structured extraction: smaller model, far more tokens-per-minute
      # headroom, so it stops competing with the summary for the budget.
      OPENAI_EXTRACTION_MODEL: ${OPENAI_EXTRACTION_MODEL:-gpt-5.6-luna}
      OPENAI_EMBED_MODEL: ${OPENAI_EMBED_MODEL:-text-embedding-3-small}
      # Speech-to-text is chosen separately from the LLM: AssemblyAI
      # diarizes, Whisper does not.
      TRANSCRIPTION_PROVIDER: ${TRANSCRIPTION_PROVIDER:-auto}
      ASSEMBLYAI_API_KEY: ${ASSEMBLYAI_API_KEY:-}
      ASSEMBLYAI_MODEL: ${ASSEMBLYAI_MODEL:-universal-3-5-pro}
      ASSEMBLYAI_FALLBACK_MODEL: ${ASSEMBLYAI_FALLBACK_MODEL:-universal-2}
      ASSEMBLYAI_LANGUAGE: ${ASSEMBLYAI_LANGUAGE:-}
      S3_ENDPOINT: ${S3_ENDPOINT:?set it in .env}
      # Where AssemblyAI fetches a recording from when it fetches one itself,
      # which saves moving the whole file through this service twice. Distinct
      # from S3_ENDPOINT, which is reachable only inside this network: a
      # presigned URL signed against http://minio:9000 is valid and
      # unreachable, which is the most confusing possible failure.
      #
      # R2's endpoint is reachable from the internet, so AssemblyAI can fetch
      # the recording itself and the file stops crossing this service twice.
      # This was blank under MinIO, whose endpoint resolved nowhere useful.
      S3_PUBLIC_ENDPOINT: ${S3_PUBLIC_ENDPOINT:-}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY:?set it in .env}
      S3_SECRET_KEY: ${S3_SECRET_KEY:?set it in .env}
      S3_BUCKET: ${S3_BUCKET:-reverie}
      S3_REGION: ${S3_REGION:-auto}
      # RAG: direct Postgres access for transcript chunk storage/retrieval.
      PG_HOST: ${PG_HOST:?set it in .env}
      PG_PORT: "${PG_PORT:-5432}"
      # Neon names its database `neondb`, not `reverie`. Parameterised rather
      # than hardcoded because the failure is a connection error naming a
      # database that was never meant to exist here.
      PG_DATABASE: ${PG_DATABASE:-reverie}
      # Unprivileged, same reason as the backend: a superuser connection would
      # silently ignore every row-level security policy.
      PG_USER: ${APP_DB_USER:-reverie_app}
      PG_PASSWORD: ${APP_DB_PASSWORD:-reverie_app}
      # "prefer" downgrades to plaintext when the server allows it, which the
      # local container does. Neon refuses unencrypted connections outright.
      PG_SSLMODE: ${PG_SSLMODE:-prefer}

  frontend:
    build:
      context: ./frontend
      # Inlined into the client bundle at build time; the runtime block below
      # cannot substitute for these (see frontend/Dockerfile).
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8080}
        NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-http://localhost:8080/ws}
        NEXT_PUBLIC_AUTH_MODE: ${NEXT_PUBLIC_AUTH_MODE:-clerk}
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
        # Reverie hosts both screens itself (app/sign-in, app/sign-up). Without
        # these Clerk's components link to its hosted pages on accounts.dev,
        # which is a different domain and a route out of the product.
        NEXT_PUBLIC_CLERK_SIGN_IN_URL: /sign-in
        NEXT_PUBLIC_CLERK_SIGN_UP_URL: /sign-up
        # `git rev-parse --short HEAD` from your shell, or nothing — the footer
        # says "dev build" when it is absent rather than showing a fake hash.
        NEXT_PUBLIC_APP_VERSION: ${NEXT_PUBLIC_APP_VERSION:-0.1.0}
        NEXT_PUBLIC_BUILD_SHA: ${NEXT_PUBLIC_BUILD_SHA:-}
        NEXT_PUBLIC_TERMS_URL: ${NEXT_PUBLIC_TERMS_URL:-}
        NEXT_PUBLIC_PRIVACY_URL: ${NEXT_PUBLIC_PRIVACY_URL:-}
    ports:
      - "3000:3000"
    depends_on:
      - backend-spring
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8080
      NEXT_PUBLIC_WS_URL: http://localhost:8080/ws
      # Defaults to clerk, not dev. An unset variable must not be able to put
      # the app into the mode that trusts an X-Dev-User header; see
      # lib/auth-store.ts and AuthenticationFilter, which default the same way.
      NEXT_PUBLIC_AUTH_MODE: ${NEXT_PUBLIC_AUTH_MODE:-clerk}
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
      # Server-side only, and deliberately not a build arg: middleware.ts runs
      # in Node and `clerkMiddleware` needs it to verify a session before a page
      # is sent. A NEXT_PUBLIC_ name would inline it into the browser bundle.
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY:-}
      NEXT_PUBLIC_CLERK_SIGN_IN_URL: /sign-in
      NEXT_PUBLIC_CLERK_SIGN_UP_URL: /sign-up

